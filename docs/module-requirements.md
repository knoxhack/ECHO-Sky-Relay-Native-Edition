# Module Requirements

Required protocol module: `echoskyrelayprotocol` version `0.1.0`.

Runtime dependencies are declared by the protocol descriptor in
`ECHO-Modules/addons/echoskyrelayprotocol/src/main/resources/META-INF/echo.mod.json`.

